Best Practice:
In the project "demo_project_{user}_{branch}"

Name code submodules with their tag, eg "bwa_and_sam_v1.4"

What comes across:
name
params (but compiled into the run)
inputs (but compiled)
outputs


{
	"name": "bwa",
	"label": "Root Intrusion",
	"icon": { "fontawesome": "\uf071" },
	"categoryId": "Security",

LATER	"params": {
		"some_name": "default value"
	},

	"inputs": {
		"extracted_reference_genome": {
MAYBE			"folder": true,
			"type": "NCBI_extracted_tar"
		},
		"fastq": {
NO			"extension": [ ".fastq", ".fq" ],
			"type": "fastq"
		}
	},
	"outputs": {
		"out.sai": {
			"type": "sai"
		}
	},
	"command": "python ./code/bwa_and_sam/bwa_tool.py",
	"command": "python ./code/bwa_and_sam_v1.4/bwa_tool.py",
	"git": [
		{
			"comment": "This is how git will work",
			"name": "./code/tools",
			"url": "git://github.com/appsoma/example_code.git"
		}
	],
	"container_image": "zsimpson/cutadapt_macs2_bwa",
	"requirements": {
		"cpus": 1,
		"mem": 512,
		"disk":512
	}
}
